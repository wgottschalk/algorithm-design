defmodule StableMatching.Preferences do
  @moduledoc """
  This is a list of preferences representing by an adjacency matrix. Consider two groups A and B:
  preference[0][1] maps to a weight for A0's preference to B1.
  """

  @doc """
  returns the index of the highest preference for an item. This index maps to an item an a second group.

    Examples:

    item = 
  """
end
